把这篇随笔的配图放在这个文件夹里，文件名要和 Markdown 里引用的一致：

  waypoints.png    —— 四路径点轨迹图（无约束 vs corridor 约束）
  iterations.png   —— 时间重分配的迭代收敛图

放好后刷新页面即可自动显示；在放进来之前，页面会显示“配图待补充”占位。
支持 png / jpg / svg 等常见格式（改扩展名时记得同步改 src/essays/minimum-snap-se3.md 里的引用）。
